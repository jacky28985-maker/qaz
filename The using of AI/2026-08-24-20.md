# 2026-08-24 20 时 AI 使用记录

本小时继续完成 InRead 2.4 的可用性与连接修复：

- 针对 `1266 x 850` 等非 16:9 桌面屏幕，计划页和训练页新增视口识别：比例低于 `1.68` 或可用高度低于 `820px` 时，自动改为整页滚动并取消双栏内部滚动，避免内容被裁切或困在独立滚动条中。
- 更新计划与训练页的前端资源版本，重新构建并部署 `inread:2.4.7` 到 `snxy-dev` 的 `/vepfs/queue010/team2`；容器以持久化数据卷继续运行在 `13000` 端口。
- 排查并修复 Windows 与 macOS SSH 连接脚本仍指向旧远端端口 `3000` 的问题，统一改为 `13000`；Windows 脚本会在确认监听进程为 `ssh.exe` 时清理旧的失效隧道。
- 使用临时本地 SSH 隧道验证登录页面返回 HTTP 200 且包含 InRead 内容，随后已清理该测试隧道。
