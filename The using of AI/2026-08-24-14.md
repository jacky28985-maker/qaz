# AI 使用记录 - 2026-08-24 14:00

## 本小时增量

- 将 InRead 2.0 部署到 `snxy-dev` 的 `/vepfs/queue010/team2`，以 Next.js 服务和 Docker 容器运行；内网服务绑定在 `192.168.12.124:3000`。
- 验证当前 `training` 网络无法路由到开发机私网 `192.168.12.0/20`，因此私网 IP 的多个端口均不可用；同时验证了本地 SSH 转发方案。
- 新增 Windows 无签名批处理启动器与 macOS `.command` 启动器，提供本机 `127.0.0.1:13000` 的 SSH 隧道访问。
- 建立临时 HTTPS 公网入口，并在地址失效后重新生成并验证新的入口地址。
- 改进 macOS 启动器：不依赖 Windows 的 SSH Host 别名，直接使用公网 SSH 地址、端口与授权私钥路径，并输出可诊断的失败信息。

## 验证

- Next.js 健康接口与搜索页在开发机和公网入口均返回正常。
- 部署容器配置为 `unless-stopped` 自动重启。
